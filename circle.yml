dependencies:
  pre:
    - sudo pip install -U platformio
    - platformio lib -g install 1
    - platformio lib -g install 2
test:
  override:
    - platformio ci ./src/Temperature.ino --board=sparkfun_redboard
